apiVersion: v1
kind: Service
metadata:
  name: findings-api
  namespace: findings
  labels:
    app.kubernetes.io/name: findings-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: findings-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
